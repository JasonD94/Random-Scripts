Various shortcuts I setup on my Linux machines.
I'll be setting them up as I go and making them the 
same on every machine, laptop/desktop/etc.

*** WINDOW KEY SHORTCUTS **
Win Z => Lock screen
Win X => xkill
Win C => 
Win V =>
Win B =>
Win A => Maximize Windows
Win S => 
Win D => 
Win F => 
Win G => 
Win Q => 
Win W => Launch Chrome
Win E => File Manager
Win R => Application finder
Win T => Terminal

** ALIASES - see .bash_aliases and .bashrc **

CD / LS
alias cu='cd ..'
alias ll='ls -alh'
alias la='ls -A'
alias l='ls -CFlh'

Git
alias a="git add -A"
alias b="git branch"
alias co="git checkout"
alias cm="git commit -m"
alias d="git diff"
alias p="git push origin"
alias s="git status"

Misc found on the internet
alias diskspace="du -S | sort -n -r |more"
alias folders="find . -maxdepth 1 -type d -print | xargs du -sk | sort -rn"
alias frak="fortune"